# iOSアプリ公開（App Store配信）までの詳細な流れ

iOSアプリをApp Storeで公開するまでの全体像と、具体的な手順をまとめました。

## 1. 事前準備
アプリを提出する前に、以下の準備が必要です。
- **Apple Developer Programへの登録**: 年間99ドルのメンバーシップ登録が必要です。
- **アセットの準備**:
  - アプリアイコン（1024x1024px, アルファチャンネルなし）
  - App Store掲載用のスクリーンショット（最低でも6.5インチと5.5インチのiPhoneサイズが必要）
- **Webページの準備**:
  - プライバシーポリシーのURL（Web上で閲覧できる状態にする）
  - サポートページのURL
- **メタデータの準備**: アプリ名、サブタイトル、概要（説明文）、キーワード。

## 2. Signing & Capabilities の設定 (Xcode / Apple Developerサイト)
Apple Developerサイトでの手動設定も可能ですが、最近はXcodeの自動管理を利用するのが一般的です。
1. Xcodeでプロジェクトを開き、ターゲットの「Signing & Capabilities」タブを開く。
2. **Automatically manage signing** にチェックを入れる。
3. Apple Developer Programに登録しているApple IDのアカウント（Team）を選択する。
4. Bundle Identifier が一意のものであることを確認する（例: `com.yourname.ColorGridPomodoro`）。

## 3. App Store Connect でのアプリ登録
1. [App Store Connect](https://appstoreconnect.apple.com/) にログイン。
2. 「マイApp」画面から「+」ボタンを押し、「新規App」を選択。
3. プラットフォーム（iOS）、アプリ名、プライマリ言語、バンドルID（手順2で設定したもの）、SKU（任意の識別用文字列）を入力して作成。
4. **App情報の設定**: プライバシーポリシーURLや、アプリのカテゴリを選択。
5. **価格および配信状況**: アプリを無料にするか有料にするか、配信する国を選択。
6. **Appのプライバシー**: アプリがどのようなユーザーデータを収集しているか（Nutrition Labels）の質問に回答する。収集していない場合はその旨を回答。

## 4. バージョン情報の入力 (App Store Connect)
「1.0 提出準備中」というメニューから、ストアに表示される情報を埋めます。
- 用意したスクリーンショットとアプリアイコンをアップロード。
- プロモーションテキスト、概要、キーワード、サポートURLを入力。
- レーティング（年齢制限指定）の質問項目に回答。

## 5. アプリのアーカイブとアップロード (Xcode)
1. Xcodeの上部バーのシミュレータ選択部分で、「Any iOS Device (arm64)」を選択する。
2. メニューバーから `Product` > `Archive` をクリック。
3. ビルドが成功すると、**Organizer** ウィンドウがポップアップします。
4. 作成されたアーカイブを選択し、右側の「Distribute App」ボタンをクリック。
5. 「App Store Connect」を選択し、案内に従って進める（通常はUploadを選択）。
6. 検証とアップロードが行われ、成功画面が出たら完了です。

## 6. TestFlightでのテスト（推奨）
アップロードしたビルドは、少し待つとApp Store Connectの「TestFlight」タブに表示されます。
- **内部テスト**: 開発チーム内のメンバーをテスターとして登録し、すぐに実際の端末（TestFlightアプリ経由）でインストールして動作確認ができます。
- 本番提出前に、実機テスト用として一度こちらを通すことを強く推奨します。

## 7. 審査への提出 (Submit for Review)
1. App Store Connectの「1.0 提出準備中」のページに戻ります。
2. ページ中段の「ビルド」セクションで、横の「+」ボタンを押し、先ほどXcodeからアップロードしたビルドを選択します。（※アップロード直後は処理中となり選択できないので、しばらく待つ必要があります）
3. 全ての必須入力項目が埋まっていることを確認し、右上の「審査へ提出 (Submit for Review)」をクリック。
4. 輸出コンプライアンス（暗号化機能の使用有無など）といったいくつかの確認画面に答えます。
5. ステータスが「審査待ち (Waiting for Review)」になれば提出完了です。

## 8. 審査結果とリリース
- **審査中 (In Review)**
  - ステータスが変わり、Appleのレビュー担当者が実際にアプリを操作してガイドラインに違反していないかチェックします。数時間〜数日かかります。
- **承認 (Pending Developer Release / Ready for Sale)**
  - 審査を通過した場合。提出時の設定（自動リリース、日時指定、手動リリース）に従ってApp Storeに公開されます。公開後、ストアに反映されるまで最大24時間程度かかることがあります。
- **リジェクト (Rejected)**
  - ガイドライン違反や、クラッシュするなどの問題が見つかった場合。問題の詳細（スクリーンショット付きのことが多い）がメッセージセンターに届きます。
  - プログラムを修正して再度手順5(アーカイブ)からやり直すか、メタデータの修正のみで済む場合は修正して再提出します。
