# アプリ仕様書：色彩学習ポモドーロタイマー（Color Grid Pomodoro）

## 1. コンセプト

* **目的**: 25分間のポモドーロ（集中時間）を通じて、5つの主要な配色理論と12色相の組み合わせを、全画面の「色彩体験」として直感的に学習する。
* **UI原則**: 数字によるカウントダウンを排除し、画面全体の「色の分割（タイル）」と「現在の時刻」のみを表示する。

## 2. 画面構成

* **背景**: 全画面を配色理論に基づいた「タイル（色の面）」で分割。
* **中央表示**:
* 現在の時刻（例 14:30）を、非常に控えめな細いフォント（Light Gray / ガラス効果など）で表示。
* 現在適用されている配色理論名（例: TRIADIC）を極小サイズで併記。


* **色相定義**: 標準的な12色相環のカラーコード（配列）をベースとする。

## 3. ダブル・シーケンス・ロジック（時間の二重構造）

実装の核心となる2つの時間軸です。

### A. モード・シーケンス（5分周期）

25分間を5分割し、画面の「分割レイアウト（配色理論）」を切り替えます。

| 経過時間 | 配色理論モード | 画面分割（レイアウト） | 色の計算（インデックス） |
| --- | --- | --- | --- |
| **00-05分** | **COMPLEMENTARY** | 上下2分割 | `base`, `base + 6` |
| **05-10分** | **SPLIT COMP** | 上1・下左右2（3分割） | `base`, `base + 5`, `base + 7` |
| **10-15分** | **TRIADIC** | 均等3分割（縦または横） | `base`, `base + 4`, `base + 8` |
| **15-20分** | **TETRADIC** | 田の字4分割 | `base`, `base + 2`, `base + 6`, `base + 8` |
| **20-25分** | **ANALOGOUS** | 縦帯3分割 | `base`, `base + 1`, `base + 11` |

※ `base` は現在の基準色インデックス（0-11）、全ての計算は `% 12` で剰余をとる。

### B. カラー・スライド（25秒周期）

各モード内で、基準となる色（`base`）を時計回りにずらしていきます。

* **周期**: 25秒ごとに `base = (base + 1) % 12` を実行。
* **効果**: 5分間の「補色モード」の中でも、赤vs緑から始まり、青vsオレンジへと、25分間で全12色の組み合わせを網羅する。

## 4. 実装のガイドライン（確実な動作のために）

1. **シンプルなレイアウト**:
複雑な描画計算（GeometryReaderやCanvas）を避け、`VStack` / `HStack` / `ZStack` 等の標準コンポーネントの組み合わせのみでタイルを構成すること。
2. **滑らかな遷移**:
25秒ごとの色相変化、および5分ごとのモード変化時には、3〜5秒程度の `withAnimation(.easeInOut)` を適用し、色がじんわりと溶け合うように変化させること。
3. **アクセシビリティ（時刻の視認性）**:
中央の時刻表示は、背景色が何色になっても視認できるよう、背面に「ブラー（ぼかし）をかけた半透明の丸いパネル」を置くか、テキストに適切なドロップシャドウを付与すること。
4. **12色のカラー配列（参考値）**:
`[#FF0000, #FF7F00, #FFFF00, #7FFF00, #00FF00, #00FF7F, #00FFFF, #007FFF, #0000FF, #7F00FF, #FF00FF, #FF007F]`
※実装環境に合わせて適切な色空間（SRGB等）で定義。

